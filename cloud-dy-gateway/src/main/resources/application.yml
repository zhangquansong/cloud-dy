server:
  port: 8095
spring:
  application:
    name: cloud-dy-gateway
  cloud:
    nacos:
      discovery:
        server-addr: 47.110.77.37:8848
management:
  endpoints:
    web:
      exposure:
        include: '*'

logging:
  level:
    org.springframework.cloud.gateway: trace
    org.springframework.http.server.reactive: debug
    org.springframework.web.reactive: debug
    reactor.ipc.netty: debug